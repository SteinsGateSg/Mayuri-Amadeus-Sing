# Demo Model

Included files:

- `mayuri_rvc_v1_200e_55200s.pth`
- `mayuri_rvc_v1.index`

Purpose:

- reproducible demo inference
- baseline listening tests
- project page audio generation

These are not the raw training checkpoints (`G_*.pth` / `D_*.pth`).
